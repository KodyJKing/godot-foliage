# This script is attatched to a leaf material shader.
# It updates the camera transform uniform so billboards can render properly in shadow passes.

extends ShaderMaterial

# # Run before rendering.
# func _process(_delta):
	
#     # Set the camera transform uniform.
#     set_shader_parameter("_camera_transform", camera_transform)
 
